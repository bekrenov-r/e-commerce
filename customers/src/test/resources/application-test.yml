spring:
  application:
    name: customers
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:tc:postgresql:16.4:///test-db?TC_INITSCRIPT=data.sql&TC_DAEMON=true
  jpa:
    hibernate:
      ddl-auto: none
eureka:
  client:
    enabled: false